# 本地环境变量文件（包含 Supabase 的私钥，绝对不能上传）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 依赖包文件夹（不需要上传，线上会自动 npm install）
node_modules/
/dist
/build

# 缓存和日志
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vite

# 操作系统临时文件
.DS_Store
Thumbs.db